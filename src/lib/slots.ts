/**
 * DOM ids for the shell's layout slots. Pages render their own filters and
 * section lists into these with a portal, which keeps that state inside the
 * page component instead of lifting it into the shell.
 */
export const SIDEBAR_ID = 'sidebar-slot';
